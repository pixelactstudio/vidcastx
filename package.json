{
  "name": "vidcastx",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:app": "turbo watch dev -F app...",
    "dev:api": "turbo watch dev -F api...",
    "start": "turbo start",
    "test": "turbo run test",
    "lint": "turbo run lint --continue -- --cache --cache-location .cache/.eslintcache",
    "lint:fix": "turbo run lint --continue -- --fix --cache --cache-location .cache/.eslintcache",
    "lint:ws": "pnpm dlx sherif@latest",
    "check-types": "turbo check-types",
    "format": "turbo run format --continue -- --cache --cache-location .cache/.prettiercache",
    "format:fix": "turbo run format --continue -- --write --cache --cache-location .cache/.prettiercache",
    "clean": "git clean -xdf node_modules .turbo",
    "clean:workspaces": "turbo run clean",
    "bump-deps": "npx npm-check-updates --deep -i",
    "bump-ui": "npx shadcn@canary add --all --overwrite -c packages/ui",
    "db:migrate": "turbo db:migrate",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:studio": "turbo db:studio",
    "postinstall": "pnpm lint:ws",
    "prepare": "husky"
  },
  "devDependencies": {
    "@turbo/gen": "^2.10.5",
    "@vidcastx/eslint-config": "workspace:*",
    "@vidcastx/prettier": "workspace:*",
    "@vidcastx/typescript-config": "workspace:*",
    "eslint": "^9.39.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "turbo": "^2.10.5",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@11.15.0",
  "engines": {
    "node": ">=22.13"
  },
  "prettier": "@vidcastx/prettier",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*",
    "workers/*"
  ],
  "overrides": {
    "@types/minimatch": "5.1.2"
  }
}
